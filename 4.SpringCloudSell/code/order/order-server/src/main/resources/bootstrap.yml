spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      label: master
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/